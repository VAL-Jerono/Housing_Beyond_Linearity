# Preview inspection notes

The app renders successfully in the local browser preview after the production build. The hero section presents the housing-linearity story with the generated California housing atlas asset, clear dark text on a warm high-key background, and an evidence stamp showing the best model, XGBoost, with R² = 0.7374. Navigation links, evidence cards, the model comparison section, the model ledger controls, linearity explanation, diagnostic charts, and stakeholder cards are present in the extracted page content.

Build status: `pnpm build` completed successfully. Vite reported a non-blocking bundle-size warning because the page uses Recharts and the embedded data tables, but the app compiled and emitted production files.
